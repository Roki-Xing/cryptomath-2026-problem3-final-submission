# Evidence Scope

## What is closed

- The submitted `submit.txt` is unchanged and hash-pinned.
- Full exact-way2 recomputation closes the way-2 mathematical and numerical evidence chain.
- All 4760 unique `(r,u)` columns were recomputed with two exact integer backends.
- All 138338 frozen endpoints matched the submitted way-2 values.
- Strategy-B Stage-A validates bounded way-1 batch tooling only.
- 18 way-1 full-domain spotchecks matched the way-2 certified values.

## What is not claimed

- This repository does not claim full way-1 `VT` provenance is closed.
- This repository does not claim `submit.txt` was regenerated by Strategy-B.
- This repository does not claim Stage-B or a full `2^32` × 138338 way-1 run was started.
- This repository does not claim official permission to replace an actually executed way-1 `VT` chain with exact-way2.

## Boundary flags

```text
stage_b_authorized=false
full_2_32_run_started=false
full_138338_way1_started=false
new_way1_run_started=false
strategy_b_final_file_generated=false
submit_txt_modified=false
vt_provenance_closed=false
```

## Official boundary

Way 1 is the exact-enumeration reference path for `VT`.
Way 2 is the submitted approximation / route-shell computation path for `VE`.
This repository preserves the distinction and states only the evidence actually provided.
