# Claims and Evidence Map

本文档将最终论文中的主要数学与实验声明映射到代码、artifact 和可复现命令。它不是新的实验结果，只是最终证据链索引。

| claim | math basis | artifact/source file | verification command | expected output |
|---|---|---|---|---|
| final 查询基线由当前 `submit.txt` 确定性冻结 | 查询坐标只从冻结提交解析，按数值 `(r,u,v)` 排序，`(r,u)` 去重后排序 | `experiments/freeze_baseline.py`; `experiments/frozen/BASELINE.json`; `experiments/frozen/SHA256SUMS.txt` | `python3 -X utf8 experiments/freeze_baseline.py && (cd experiments/frozen && sha256sum -c SHA256SUMS.txt)` | contains `final_queries_rows=138338`, `final_ru_rows=4760`, all listed hashes pass |
| 冻结基线绑定未修改的提交文件 | SHA-256 直接覆盖 `submit.txt` 原始字节 | `submit.txt`; `experiments/frozen/BASELINE.json` | `sha256sum submit.txt && python3 -c "import json; print(json.load(open('experiments/frozen/BASELINE.json'))['source']['sha256'])"` | both are `7b0f638ba8678462ee8d6c12bc0c5b89d7354b4a095b31330f3ba495acfe2e2e` |
| 无截断认证定理的三个代码条件可审计 | 定理 1：完整保留局部 S 盒分支、S 层元组和后继状态时，有限支撑状态表满足 \(A_r=R^r e_u\) | `experiments/submit_audit.csv`; `experiments/audit/submit_audit_summary.json` | `python3 -c "import csv; h=next(csv.reader(open('experiments/submit_audit.csv', newline='', encoding='utf-8'))); print([c in h for c in ['certified_no_truncation','round_stats','ve_matches_submit']])"` | `[True, True, True]` |
| way-2 audit 不虚构 way-1 exact provenance | audit 只记录实际执行的 estimator；未执行 exact 时显式标记 false、空命令和无结果 | `experiments/audit_submit.py`; `experiments/submit_audit.csv` | `python3 -c "import csv; rs=list(csv.DictReader(open('experiments/submit_audit.csv', newline='', encoding='utf-8'))); print(len(rs), all(r['way2_executed']=='1' and r['way2_value_source']=='estimator' and r['submitted_vt_field_source']=='submit.txt' and r['exact_executed']=='0' and r['exact_command']=='' and r['exact_result_available']=='0' for r in rs))"` | `138338 True` |
| final score 来自评分程序 | 评测计分规则逐行求和 | `submit.txt`; `score` | `./score --dedup uv --positive-only submit.txt` | contains `valid_count=138338` and `total_score=105843.622442471292742994` |
| `submit.txt` 可由 way-2 source 重建 | 最终提交由认证 source rows 构造并按 `(u,v)` 去重 | `experiments/build_submit_from_sources.py`; source CSVs; `submit.txt` | `python3 -X utf8 experiments/build_submit_from_sources.py --out /tmp/submit_rebuilt.txt && cmp submit.txt /tmp/submit_rebuilt.txt` | command exits 0 |
| 全部提交记录通过无截断认证且 VE 复现无不匹配 | 定理 1 的代码级假设和 way-2 复算 | `experiments/audit/submit_audit_summary.json`; `experiments/submit_audit.csv` | `python3 -c "import json; d=json.load(open('experiments/audit/submit_audit_summary.json')); print(d['audit_rows'], d['certified_no_truncation_rows'], d['ve_mismatch_rows'], d['duplicate_uv_rows'])"` | `138338 138338 0 0` |
| E06 contributes 384 final rows and 6 unique input masks | E06 selected rows are certified r=3 active-2 rows merged through the final source builder | `experiments/manifests/E13_final_integration.md`; `experiments/new_sweeps/r3_active2_lat/r3_active2_lat_cert_u2020shift_u4040shift_top64_beam200k_trans100k.csv`; `experiments/new_sweeps/r3_active2_lat/r3_active2_lat_cert_u2020_u4040_top64_beam200k_trans100k.csv`; `experiments/new_sweeps/r3_active2_lat/r3_active2_lat_cert_u60600000_u006060_top64_beam200k_trans100k.csv` | `grep -E "E06 merged rows|r=3 active-2 unique u" experiments/manifests/E13_final_integration.md` | contains `E06 merged rows: 384` and `r=3 active-2 unique u: 6` |
| exact spotcheck has 18 rows, 0 mismatch, and E06=4 | way-1 exact enumeration is validation-only, uses its own files/schema, and checks selected records | `experiments/spotcheck/exact_spotcheck.csv`; `experiments/spotcheck/exact_spotcheck_summary.json`; `experiments/spotcheck/exact_spotcheck_queries.csv` | `python3 -c "import csv,json; d=json.load(open('experiments/spotcheck/exact_spotcheck_summary.json')); q=sum(1 for r in csv.DictReader(open('experiments/spotcheck/exact_spotcheck_queries.csv', newline='', encoding='utf-8')) if r['category']=='r3_active2_e06'); print(d['count'], d['mismatch_count'], d['max_abs_err'], d['max_rel_err'], q)"` | `18 0 0.0 0.0 4` |
| complexity table uses unique `(r,u)` units | 方式二复杂度单位是固定输入列的路线壳展开，而不是每个 `(r,u,v)` 输出记录 | `experiments/complexity/complexity_summary.json`; `experiments/complexity/complexity_by_round.json`; `experiments/submit_audit.csv` | `python3 -c "import json; d=json.load(open('experiments/complexity/complexity_summary.json')); print(d['unique_ru'], d['max_generated_transitions_per_ru'], round(d['generated_transitions_ratio_to_2_32_max'], 8), d['ru_generated_transitions_ge_2_32'], d['ru_expanded_states_ge_2_32'])"` | `4760 7578152 0.00176443 0 0` |
| 高分路线贡献表来自 estimator trace | 同端点线性路线贡献带符号聚合 | `experiments/traces/high_score_r2_00002000_08880000_trace.txt`; `experiments/traces/high_score_r2_00002000_08880000_trace.md` | `grep -e "VE     =" -e "score\\* =" -e "routes =" -e "route 1:" experiments/traces/high_score_r2_00002000_08880000_trace.txt` | contains `VE     = 1`, `score* = 4`, `routes = 4`, and route contribution lines |
| VT/VE equal numeric fields are not copied from way-1 | certified way-2 complete route-shell summation yields the submitted coordinate value | `docs/VT_VE_COMPLIANCE.md`; `README.md`; `REPORT.md` | `grep -R -e "can safely se[t]" -e "setting V[T]" -e "set VT=V[E]" -e "直接令.*V[T]" -e "直接令.*V[E]" README.md REPORT.md docs paper *.md || true` | no output |
